# Used client-side (baked in at build time) and as a fallback for server routes
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-side only — what Next.js API routes actually call internally
API_GATEWAY_URL=http://localhost:8000

# Set to "true" only when served over HTTPS, otherwise login cookies won't be stored
COOKIE_SECURE=false