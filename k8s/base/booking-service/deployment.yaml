apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
spec:
  replicas: 1
  selector:
    matchLabels: { app: booking-service }
  template:
    metadata:
      labels: { app: booking-service }
    spec:
      containers:
        - name: booking-service
          image: booking-service:local
          imagePullPolicy: Never
          ports: [{ containerPort: 5003 }]
          envFrom:
            - configMapRef: { name: booking-service-config }
            - secretRef: { name: booking-service-secret }
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { cpu: 200m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 5003 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 5003 }
            initialDelaySeconds: 15
            periodSeconds: 20